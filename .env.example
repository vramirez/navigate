ADMIN_EMAIL=admin@navigate.co
ADMIN_PASSWORD=changeme123
JWT_SECRET=replace-with-a-long-random-secret
PORT=3000
NODE_ENV=production

# Restrict CORS to your domain in production (default is * which allows all origins)
CORS_ORIGIN=https://yourdomain.com

# DB_PATH=./data/navigate.db  # optional, defaults to ./data/navigate.db
